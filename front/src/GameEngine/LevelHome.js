import { Scene } from 'phaser';

export default class LevelHome extends Scene {
	constructor(config) {
		const sceneConfig = {};
		super(sceneConfig);
		this.config = config;
  }
  
  // Function du jeu

	preload() {}

	create() {}

	update() {}
		
}
